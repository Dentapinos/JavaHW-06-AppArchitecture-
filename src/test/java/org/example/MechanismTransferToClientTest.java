package org.example;

import org.junit.jupiter.api.DisplayName;
import org.junit.jupiter.api.Test;

import static org.junit.jupiter.api.Assertions.assertEquals;

@DisplayName("Механизм передачи денег от сейфа к клиенту")
class MechanismTransferToClientTest {

    @DisplayName("Какой-то успешный тест")
    @Test
    void shouldReturnEmptyList() {
        assertEquals(0, 0);
    }
}